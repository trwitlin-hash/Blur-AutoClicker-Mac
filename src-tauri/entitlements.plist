<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for CGEventPost (synthetic mouse clicks) -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
    <!-- Disable the App Sandbox so CGEventPost with kCGHIDEventTap is not blocked -->
    <key>com.apple.security.app-sandbox</key>
    <false/>
    <!-- Allow JIT for the WebView runtime -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow unsigned executable memory (needed by some JS engines) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Disable library validation so dylibs can load -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
